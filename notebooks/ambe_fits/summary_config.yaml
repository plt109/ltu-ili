# summary_config.yaml
save_base: 3_param_trained_models/summary_sweep

data:
  base_dir: /home/puehlengt/appletree/notebooks
  fname: harvested_testsims_3params.npy
  num_samples: 50000

model:
  type: nsf
  hidden_features: 32
  num_transforms: 2

training:
  batch_size: 256
  learning_rate: 0.0001
  stop_after_epochs: 100
  clip_max_norm: 5
  max_epochs: 10000
  weight_decay: 0.0
